# List Tariff Types

List Tariff Types

## Usage

``` r
tariff_types
```

## Format

An object of class `data.table` (inherits from `data.frame`) with 7 rows
and 3 columns.

## Examples

``` r
str(entsoeapi::tariff_types)
#> Classes ‘data.table’ and 'data.frame':   7 obs. of  3 variables:
#>  $ code       : chr  "A01" "A02" "A03" "A04" ...
#>  $ title      : chr  "Winter HT" "Winter HHT" "Winter NT" "Summer HT" ...
#>  $ description: chr  "Winter HT tariff." "Winter HHT tariff." "Winter NT tariff." "Summer HT tariff." ...
#>  - attr(*, ".internal.selfref")=<externalptr> 
```
