% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{there_is_provider}
\alias{there_is_provider}
\title{Check if the ENTSO-E API provider is reachable}
\usage{
there_is_provider(
  api_scheme = .api_scheme,
  api_domain = .api_domain,
  api_name = .api_name
)
}
\arguments{
\item{api_scheme}{Character. URL scheme, default \code{"https://"}.}

\item{api_domain}{Character. API host, default \code{"web-api.tp.entsoe.eu/"}.}

\item{api_name}{Character. API path prefix, default \code{"api?"}.}
}
\value{
A single logical value.
}
\description{
Sends a probe request to the ENTSO-E Transparency Platform API and returns
\code{TRUE} if the server responds with HTTP 401 Unauthorized (meaning the
endpoint is up but the dummy token was rejected). Returns \code{FALSE} when there
is no internet connection or when the server is unreachable. Primarily
intended as an \verb{@examplesIf} guard in package documentation.
}
\examples{
there_is_provider()

}
